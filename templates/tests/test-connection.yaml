apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "free-batch.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "free-batch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "free-batch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
